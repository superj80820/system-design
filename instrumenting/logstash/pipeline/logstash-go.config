input {  
  beats {
    port => 5044
  }
}

filter {
  json {
    source => "message"
  }
}

output {
  elasticsearch {
    hosts => ["http://docker.for.mac.localhost:9200"]
    index => "go"
  }
  stdout {
    codec => rubydebug
  }
}